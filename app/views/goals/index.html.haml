%h1 Goals index

- @goals.each do |g|
  - if !g.status?
    %p= link_to g.decname, goal_path(g)
    %p= g.description
    %h3 Status
    %p= g.percentage
    %h3 Tasks
    %ul
      = g.tasks_for_index

%br
%br
%br

= link_to "+ New goal", new_goal_path(@goal)
