!!!
%html
  %head
    %title Achievr
    = stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
    %link{href: "http://fonts.googleapis.com/css?family=Roboto:400,300italic,300,400italic,700,700italic|Roboto+Condensed:300italic,400italic,700italic,400,700,300", rel: "stylesheet", type: "text/css"}/
  %body
    #wrap
      .top
      .bar

      .circle
        = link_to image_tag( '/images/achieve-plus.png'), new_goal_path(@goal), :class => 'achieve'
      .wrapper

        %nav
          %ul
            - if current_user.present?
              %li= link_to 'Home', '/goals'
              %li= link_to 'Completed goals', '/completed-goals'
            - else
              %li= link_to 'Home', root_path
            %li= link_to 'About', '/about'
            %li= link_to 'Contact', '/contact'
            = render 'devise/menu/login_items'

        - if notice
          %p.alert.alert-success= notice
        - if alert
          %p.alert.alert-danger= alert

        = yield
    #push

  %footer.footer
    .wrapper
